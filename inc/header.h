#ifndef HEADER_H
#define HEADER_H

typedef struct s_player {
    int x;
    int y;
    int cloneX;
    int cloneY;
    int dx;
    int dy;
} t_player;
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

#endif